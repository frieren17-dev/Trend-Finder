# Optional: base URL of the Flask backend.
# Leave unset to use the Vite dev proxy (recommended for local dev).
# Set this to call the API directly (CORS is enabled server-side):
# VITE_API_BASE=http://localhost:5000
